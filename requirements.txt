fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.0
aiofiles==24.1.0
slowapi==0.1.9
aiohttp==3.10.0
httpx==0.27.0
torch==2.4.0
transformers==4.45.0
# Optional: Redis caching (one-line switch via REDIS_URL env var)
# redis==5.0.0
# Optional: BeautifulSoup for live ETL scraping
# beautifulsoup4==4.12.0
# duckduckgo-search requires Rust/Cargo (pyreqwest-impersonate) — install manually if web search needed
# pip install duckduckgo-search
